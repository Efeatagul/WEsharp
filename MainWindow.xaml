<Window x:Class="WSharp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WNEURA Simulation Control" Height="600" Width="900"
        Background="#1E1E1E" Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="WNEURA" FontSize="32" FontWeight="Bold" Foreground="#007ACC"/>
            <TextBlock Text="Advanced Simulation Platform" FontSize="14" Foreground="#AAAAAA"/>
        </StackPanel>

        <!-- Output Area -->
        <Border Grid.Row="1" Background="#252526" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="SIMULATION OUTPUT (JSON)" Foreground="#808080" FontSize="12" Margin="0,0,0,10"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="OutputText" Background="Transparent" Foreground="#D4D4D4" 
                             BorderThickness="0" TextWrapping="Wrap" IsReadOnly="True"
                             FontFamily="Consolas" FontSize="13"
                             Text="Ready to start simulation..."/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Controls -->
        <Button Grid.Row="2" x:Name="RunButton" Content="RUN SIMULATION" 
                Click="RunButton_Click"
                Height="50" Margin="0,20,0,0"
                Background="#007ACC" Foreground="White" BorderThickness="0"
                FontSize="16" FontWeight="SemiBold" Cursor="Hand">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Button.Resources>
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#005F9E"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="#333333"/>
                            <Setter Property="Foreground" Value="#666666"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
